# Docker Testing Environment Configuration
# This file provides mock values for testing without real Notion setup

# Mock Notion API Configuration (for testing)
NOTION_API_KEY=mock_key_for_docker_testing_replace_with_real_key_for_production
VENDORS_DB_ID=mock_vendors_database_id
PARTS_DB_ID=mock_parts_database_id  
SCORES_DB_ID=mock_scores_database_id

# Flask Configuration (Production Mode)
FLASK_ENV=production
FLASK_DEBUG=false

# Port Configuration (AWS App Runner compatible)
PORT=8080

# Security Configuration
SECRET_KEY=docker-test-secret-key-change-in-production
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Default Scoring Weights
DEFAULT_WEIGHTS={"total_cost": 0.4, "total_time": 0.3, "reliability": 0.2, "capacity": 0.1}

# Note: This file is for Docker testing only
# For production deployment, use AWS Secrets Manager for sensitive values